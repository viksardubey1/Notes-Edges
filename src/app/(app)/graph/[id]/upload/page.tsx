/**
 * Upload Flow Page — Notes & Edges
 * Route: /graph/:id/upload
 *
 * Rendered as a bottom sheet overlay within the workspace.
 * Three zones: drag-and-drop PDF | paste text | URL input (future).
 * Single CTA: "Generate Graph" — full-width, Arc Blue.
 *
 * Full implementation in upload phase.
 */

export default function UploadPage() {
  // This page triggers the upload sheet to open when mounted
  // and renders inside the graph workspace layout as an overlay

  return null;
}
